# Shared environment variables for local run from repository root.
# You can keep both services in one file when starting with `./mvnw -pl ...`.

# Bot
TELEGRAM_TOKEN=replace_with_real_token
APP_TELEGRAM_POLLING_ENABLED=false
APP_TELEGRAM_URL=http://127.0.0.1:65535/bot
APP_TELEGRAM_UPDATE_LISTENER_SLEEP=1s
APP_TELEGRAM_DEBUG=false

# Scrapper
STACKOVERFLOW_KEY=replace_with_stackoverflow_key
STACKOVERFLOW_ACCESS_KEY=replace_with_stackoverflow_access_key
GITHUB_TOKEN=replace_with_github_token
